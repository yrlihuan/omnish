# config/client.toml
# Client configuration — copy to ~/.omnish/client.toml

# Address of the daemon (Unix socket path or tcp://host:port)
# daemon_addr = "~/.omnish/omnish.sock"   # default

[shell]
# command = "/bin/bash"    # defaults to $SHELL
command_prefix = "::"
